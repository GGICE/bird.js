{
  "name": "bird.js",
  "version": "0.0.1",
  "private": true,
  "devDependencies": {
    "webpack": "1.13.3",
    "webpack-dev-server": "^1.14.1",
    "eslint": "^3.7.1",
    "pre-commit": "^1.1.3"
  },
  "dependencies": {
    "ts-loader": "^1.2.2",
    "typescript": "^2.0.10"
  },
  "scripts": {
    "build": "webpack -p --progress --profile --colors",
    "dev": "webpack-dev-server --hot --progress --profile --colors",
    "lintJs": "./node_modules/.bin/eslint ./src/**/*.js",
    "gitAdd": "git add ."
  },
  "pre-commit": [
    "lintJs",
    "build",
    "gitAdd"
  ]
}
